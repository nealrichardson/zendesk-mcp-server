ZENDESK_SUBDOMAIN=your-subdomain
ZENDESK_EMAIL=your-email@example.com
ZENDESK_API_TOKEN=your-api-token

# Set to 'true' to enable write tools (create, update, delete operations)
# Default is read-only mode when not set or set to any other value
ZENDESK_WRITE_ENABLED=false
